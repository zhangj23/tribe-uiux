# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Brain region mapping
nilearn>=0.11.0
numpy>=1.26.0

# Media conversion
ffmpeg-python>=0.2.0

# LLM integration
anthropic>=0.40.0

# Settings
pydantic-settings>=2.0.0

# Auth (Supabase JWT verification + service-role DB writes)
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
supabase>=2.4.0

# TRIBE v2 (install separately for real inference)
# pip install git+https://github.com/facebookresearch/tribev2.git
